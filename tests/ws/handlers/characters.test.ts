import { buildHandlers } from "../../../src/ws/handlers";

describe("ws/handlers/characters", () => {
  it("send addCharacter", () => {
    const handlers = buildHandlers();
  });

  it("send removeCharacter", () => {});
});
